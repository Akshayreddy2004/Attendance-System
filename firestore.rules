rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone who is logged in to read and write data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
